Basic framework for MDP

It has the key function prototypes that must be implemented in the class inheriting from mpd

It provides an implementation of the ValueIteration algorithm, as well as the PolicyFromValue, that implements the classic algorithms, following R&N
